#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;
vector<int> vc;
int n;

bool chk(vector<int>& vc) {
	return 0;
}

int main()
{
	cin >> n;
	for (int i = 0; i < n; i++) {
		int a;
		cin >> a;
		vc.push_back(a);
	}
	while(check())
}